/** @jsx React.DOM */
var React = require('react');

var App = module.exports = React.createClass({
  getInitialState() {
    return {};
  },

  componentDidMount() {
    // kick off loading of data?
  },

  render() {
    return (
      <h2>
        OHAI
      </h2>
    );
  }
});
